FROM fedora:43

RUN dnf install -y \
    cmake \
    gcc \
    gcc-c++ \
    make \
    qt6-qtbase-devel \
    qt6-qtwebengine-devel \
    && dnf clean all

WORKDIR /src
COPY . .

RUN cmake -B build -DCMAKE_BUILD_TYPE=Release -DMDPEEK_BACKEND=qt \
    && cmake --build build --parallel "$(nproc)"
